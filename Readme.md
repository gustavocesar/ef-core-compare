# Ef Core Method Compare

## Setup

 - Clone this project

 - Download dependencies
   - `dotnet restore`

 - Create database
   - `dotnet ef database update`

 - Run
   - `dotnet run`

 - Change `_quantity` attribute of `Services/UserProfileService`, to change the data volume of the benchmark